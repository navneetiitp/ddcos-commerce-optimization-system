# DDCOS Frontend

React + TypeScript + Vite + Tailwind + Recharts frontend for the DDCOS commerce intelligence platform.

Set `VITE_API_URL` in `.env` if the backend is not running on the local default.

```bash
npm install
npm run dev
```

Production build:

```bash
npm run build
```
